<ContentPage
    x:Class="A_Connect.Views.ProfsToPickNewsFeedPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Profs to Pick News Feed">

    <!-- Main Grid with rows for header, main content, bottom nav (optional) -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Blue Header -->
        <Grid BackgroundColor="#063970" Padding="20,40,20,20">
            <Label Text="Profs to Pick"
                   TextColor="Gold"
                   FontAttributes="Bold"
                   FontSize="24"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>
        </Grid>

        <!-- 2. Main Content -->
        <StackLayout Grid.Row="1" Spacing="15" Padding="20">

            <!-- Search Bar -->
            <Frame CornerRadius="25" BackgroundColor="White" Padding="0">
                <HorizontalStackLayout Padding="10,0">
                    <!-- Icon: Replace with your own -->
                    <Image Source="search_icon.png"
                           HeightRequest="20"
                           WidthRequest="20"
                           VerticalOptions="Center"
                           Margin="0,0,5,0"/>
                    <Entry Placeholder="Search"
                           VerticalOptions="Center"
                           FontSize="14"
                           Margin="0"/>
                </HorizontalStackLayout>
            </Frame>

            <!-- Tab-like buttons -->
            <Grid ColumnSpacing="0" RowSpacing="0" Padding="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- "Posts by other users" -->
                <Button Text="Posts by other users"
                        BackgroundColor="#F0F0F0"
                        TextColor="Black"
                        Grid.Column="0"
                        CornerRadius="0"
                        FontAttributes="Bold"/>
                <!-- "Own posts" -->
                <Button Text="Own posts"
                        BackgroundColor="#F0F0F0"
                        TextColor="Black"
                        Grid.Column="1"
                        CornerRadius="0"
                        FontAttributes="Bold"/>
            </Grid>

            <!-- List of professor reviews -->
            <CollectionView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="0,10">
                            <Frame BackgroundColor="#1F6FB7"
                                   CornerRadius="15"
                                   Padding="15">
                                <VerticalStackLayout Spacing="5">
                                    <!-- Subject or Course Code -->
                                    <Label Text="CSCI 42"
                                           TextColor="White"
                                           FontSize="16"
                                           FontAttributes="Bold"/>

                                    <!-- Professor's Name -->
                                    <Label Text="DUEÑAS, Lorenzo"
                                           TextColor="White"
                                           FontSize="14"
                                           FontAttributes="Bold"/>

                                    <!-- Stars (Placeholder) -->
                                    <HorizontalStackLayout>
                                        <Image Source="star_filled.png" WidthRequest="16" HeightRequest="16"/>
                                        <Image Source="star_filled.png" WidthRequest="16" HeightRequest="16"/>
                                        <Image Source="star_filled.png" WidthRequest="16" HeightRequest="16"/>
                                        <Image Source="star_filled.png" WidthRequest="16" HeightRequest="16"/>
                                        <Image Source="star_outline.png" WidthRequest="16" HeightRequest="16"/>
                                    </HorizontalStackLayout>

                                    <!-- Short review snippet (2 lines) -->
                                    <Label Text="Had him for CSCI 42, was very good. Often had fun icebreakers..."
                                           TextColor="White"
                                           FontSize="12"
                                           MaxLines="2"/>

                                    <!-- Date posted -->
                                    <Label Text="Date: March 2, 2025"
                                           TextColor="White"
                                           FontSize="10"
                                           HorizontalOptions="End"/>
                                </VerticalStackLayout>
                            </Frame>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- "Post a review" button (could be shown only in "Own posts") -->
            <Button Text="Post a review +"
                    BackgroundColor="#F1C40F"
                    TextColor="Black"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HorizontalOptions="Center"
                    Padding="10,5"
                    Clicked="OnPostReviewClicked"/>
        </StackLayout>

        <!-- 3. Bottom Nav Bar (optional) -->
        <Grid Grid.Row="2" BackgroundColor="#063970" HeightRequest="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Nav icons (replace with your actual icons/commands) -->
            <ImageButton Source="nav_icon1.png"
                         Grid.Column="0"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         WidthRequest="30" HeightRequest="30"/>
            <ImageButton Source="nav_home.png"
                         Grid.Column="1"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         WidthRequest="30" HeightRequest="30"/>
            <ImageButton Source="nav_profile.png"
                         Grid.Column="2"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         WidthRequest="30" HeightRequest="30"/>
        </Grid>
    </Grid>
</ContentPage>
